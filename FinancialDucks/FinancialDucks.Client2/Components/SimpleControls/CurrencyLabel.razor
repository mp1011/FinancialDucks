@if(Amount < 0)
{
    <span class="badge rounded-pill bg-danger">@((-Amount).ToString("C"))</span>
}
else
{
    <span class="badge rounded-pill bg-success">@Amount.ToString("C")</span>
}

@code
{
    [Parameter]
    public decimal Amount { get; set; } 
}
